This is just a demo repository to test some of the git features. 

# Git fork 
 * you can make a fork directly from github UI ( make sure you don't sync only one branch) 

# Git merge to a forked branch 


